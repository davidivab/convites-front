FROM node:22-alpine

WORKDIR /app

RUN apk add --no-cache libc6-compat

EXPOSE 3000

# El comando real lo define docker-compose.yml (`command:`).
# Este CMD solo aplica si se corre la imagen sin compose.
CMD ["npm", "run", "dev", "--", "--hostname", "0.0.0.0", "--port", "3000"]
